# Edit cross-tool Global Agent Instructions

Carrent's personalization setting edits the cross-tool `~/.agents/AGENTS.md` file. Agent Core reads it as global instructions before project-level `AGENTS.md`.
